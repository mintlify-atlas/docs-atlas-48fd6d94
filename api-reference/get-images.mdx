---
title: Get images
description: Fetch a collection of previously generated images from the VisionaryAI system
---

## Endpoint

```
GET /api/getImages
```

Retrieves a collection of images that have been generated by the system. This endpoint returns image metadata and URLs for all available images.

## Request

This endpoint does not require any parameters. Simply make a GET request to receive the image collection.

## Response

Returns an array of image objects. Each object contains metadata about a generated image.

<ResponseField name="images" type="array">
  Array of image objects containing metadata and URLs for generated images
</ResponseField>

## Example request

```bash
curl -X GET https://your-domain.com/api/getImages
```

## Example response

```json
{
  "images": [
    {
      "id": "img_123",
      "url": "https://example.com/image1.png",
      "prompt": "A serene mountain landscape",
      "createdAt": "2026-03-04T10:30:00Z"
    },
    {
      "id": "img_124",
      "url": "https://example.com/image2.png",
      "prompt": "A futuristic city skyline",
      "createdAt": "2026-03-04T11:15:00Z"
    }
  ]
}
```

## Response details

The response structure depends on the data returned from the underlying Azure service. The endpoint does not cache results, ensuring you always receive the latest image collection.

## Error codes

<ResponseField name="500" type="error">
  Internal server error - The image service failed to retrieve the image collection
</ResponseField>

## Usage tips

- Use this endpoint to display a gallery of generated images
- Results are not cached, so each request fetches fresh data
- The response structure may vary based on the Azure service configuration